apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-setup.fullname" . }}
  labels:
    {{- include "tenant-setup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tenant-setup.selectorLabels" . | nindent 4 }}
